# Local Miru notification settings.
# Copy this file to `.env` in the repo root and fill in your real values locally.

PUSHOVER_USER_KEY=
PUSHOVER_APP_TOKEN=
PUSHOVER_ENABLED=true
PUSHOVER_DEFAULT_PRIORITY=0
