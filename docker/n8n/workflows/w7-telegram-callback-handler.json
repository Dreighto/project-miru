{
  "name": "W7 — Telegram Callback Handler",
  "nodes": [
    {
      "parameters": {
        "updates": ["callback_query"],
        "additionalFields": {}
      },
      "id": "w7001-telegram-trigger",
      "name": "w7001-telegram-trigger",
      "type": "n8n-nodes-base.telegramTrigger",
      "typeVersion": 1.2,
      "position": [200, 300],
      "webhookId": "w7-telegram-callback",
      "credentials": {
        "telegramApi": { "id": "5tWWGl8jkCOHqJQg", "name": "telegram-n8n" }
      }
    },
    {
      "parameters": {
        "resource": "callback",
        "operation": "answerQuery",
        "queryId": "={{ $json.callback_query.id }}",
        "additionalFields": {
          "text": "Received"
        }
      },
      "id": "w7002-ack-callback",
      "name": "w7002-ack-callback",
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [400, 300],
      "credentials": {
        "telegramApi": { "id": "5tWWGl8jkCOHqJQg", "name": "telegram-n8n" }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7003-parse-payload\n// Slice the 61-byte fixed-width callback_data: T(12)A(1)N(8)S(8)H(32).\n// Anchor MUST match every mint node (see w2012a, w2w03d, fixture).\n// NOTE: must read from the trigger, not $input — w7002 (answerQuery) replaces\n// the item's json with the Telegram API response, losing the callback_query.\nconst ANCHOR_UNIX = 1767225600; // 2026-01-01T00:00:00Z\n\nconst raw = $('w7001-telegram-trigger').item.json;\nconst cq = raw.callback_query || {};\nconst cb = (cq.data || '').toString();\n\nif (cb.length !== 61) {\n  return { json: {\n    _abort: true,\n    _abort_reason: 'callback_data length mismatch: expected 61, got ' + cb.length,\n    callback_query_id: cq.id || null,\n    chat_id: (cq.message && cq.message.chat && cq.message.chat.id) || null,\n    message_id: (cq.message && cq.message.message_id) || null,\n    from_user_id: (cq.from && cq.from.id) || null,\n    raw_callback_data: cb\n  }};\n}\n\nconst token = cb.slice(0, 12);\nconst action = cb.slice(12, 13);\nconst nonce = cb.slice(13, 21);\nconst ts_hex = cb.slice(21, 29);\nconst hmac_hex = cb.slice(29, 61);\n\nconst ts_minutes = parseInt(ts_hex, 16);\nconst ts_unix_seconds = ANCHOR_UNIX + (ts_minutes * 60);\n\nreturn { json: {\n  token,\n  action,\n  nonce,\n  ts_hex,\n  hmac_hex,\n  ts_unix_seconds,\n  callback_query_id: cq.id || null,\n  chat_id: (cq.message && cq.message.chat && cq.message.chat.id) || null,\n  message_id: (cq.message && cq.message.message_id) || null,\n  from_user_id: (cq.from && cq.from.id) || null,\n  message_text: (cq.message && cq.message.text) || null,\n  raw_callback_data: cb\n}};"
      },
      "id": "w7003-parse-payload",
      "name": "w7003-parse-payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [600, 300]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7004-hmac-validate\n// Verify HMAC-SHA256(token+action+nonce+ts_hex) truncated to 16 bytes hex (32 chars).\n// Replay window: -60s (clock skew) to +600s (10 minutes).\nconst crypto = require('crypto');\n\nconst data = $input.item.json;\n\nif (data._abort) {\n  return { json: { ...data, validated: false, reject_reason: data._abort_reason } };\n}\n\nconst secret = $env.TELEGRAM_CALLBACK_SECRET;\nif (!secret || secret.length < 32) {\n  return { json: { ...data, validated: false, reject_reason: 'TELEGRAM_CALLBACK_SECRET missing or too short' } };\n}\n\nconst payload = data.token + data.action + data.nonce + data.ts_hex;\nconst expected = crypto.createHmac('sha256', secret).update(payload).digest('hex').slice(0, 32);\n\nlet hmac_ok = false;\ntry {\n  hmac_ok = crypto.timingSafeEqual(Buffer.from(expected, 'hex'), Buffer.from(data.hmac_hex, 'hex'));\n} catch (e) {\n  return { json: { ...data, validated: false, reject_reason: 'hmac comparison threw: ' + e.message } };\n}\n\nif (!hmac_ok) {\n  return { json: { ...data, validated: false, reject_reason: 'hmac mismatch' } };\n}\n\nconst now_seconds = Math.floor(Date.now() / 1000);\nconst age = now_seconds - data.ts_unix_seconds;\nif (age > 600) {\n  return { json: { ...data, validated: false, reject_reason: 'callback older than 10 min (age=' + age + 's)' } };\n}\nif (age < -60) {\n  return { json: { ...data, validated: false, reject_reason: 'callback timestamp in future (age=' + age + 's)' } };\n}\n\nreturn { json: { ...data, validated: true, callback_age_seconds: age } };"
      },
      "id": "w7004-hmac-validate",
      "name": "w7004-hmac-validate",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [800, 300]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "v1",
              "leftValue": "={{ $json.validated }}",
              "rightValue": true,
              "operator": { "type": "boolean", "operation": "true", "singleValue": true }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "w7005-validate-branch",
      "name": "w7005-validate-branch",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1000, 300]
    },
    {
      "parameters": {},
      "id": "w7-noop-rejected",
      "name": "w7-noop-rejected",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [1200, 480]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7006-lookup-pending\n// Read /miru-data/pending_callbacks.jsonl (append-only). Walk in reverse to find\n// the latest 'intent' row matching token, AND any 'decided' row for same token.\nconst fs = require('fs');\nconst PENDING = '/miru-data/pending_callbacks.jsonl';\n\nconst data = $input.item.json;\nconst targetToken = data.token;\n\nlet content = '';\ntry {\n  if (fs.existsSync(PENDING)) {\n    content = fs.readFileSync(PENDING, 'utf8');\n  }\n} catch (e) {\n  return { json: { ...data, found: false, reject_reason: 'pending_callbacks read error: ' + e.message } };\n}\n\nconst lines = content.split('\\n').filter(Boolean);\nlet intentRow = null;\nlet decidedRow = null;\nfor (let i = lines.length - 1; i >= 0; i--) {\n  let row;\n  try { row = JSON.parse(lines[i]); } catch (e) { continue; }\n  if (row.token !== targetToken) continue;\n  if (row.kind === 'decided' && !decidedRow) decidedRow = row;\n  if (row.kind === 'intent' && !intentRow) intentRow = row;\n  if (intentRow && decidedRow) break;\n}\n\nif (!intentRow) {\n  return { json: { ...data, found: false, reject_reason: 'token not found in pending_callbacks' } };\n}\n\nif (decidedRow) {\n  return { json: { ...data, found: true, already_decided: true, decided_action: decidedRow.action, decided_at: decidedRow.decided_at, intent_row: intentRow } };\n}\n\nreturn { json: {\n  ...data,\n  found: true,\n  already_decided: false,\n  trace_id: intentRow.trace_id,\n  issue_id: intentRow.issue_id,\n  issue_identifier: intentRow.issue_identifier,\n  chosen_worker: intentRow.chosen_worker,\n  confidence: intentRow.confidence,\n  risk: intentRow.risk,\n  button_set: intentRow.button_set,\n  issue_existing_label_ids: intentRow.issue_existing_label_ids || [],\n  labels_map: intentRow.labels_map || {},\n  intent_written_at: intentRow.intent_written_at\n}};"
      },
      "id": "w7006-lookup-pending",
      "name": "w7006-lookup-pending",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1200, 200]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "f1",
              "leftValue": "={{ $json.found }}",
              "rightValue": true,
              "operator": { "type": "boolean", "operation": "true", "singleValue": true }
            },
            {
              "id": "f2",
              "leftValue": "={{ $json.already_decided }}",
              "rightValue": false,
              "operator": { "type": "boolean", "operation": "false", "singleValue": true }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "w7007-found-branch",
      "name": "w7007-found-branch",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1400, 200]
    },
    {
      "parameters": {},
      "id": "w7-noop-duplicate",
      "name": "w7-noop-duplicate",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [1600, 380]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "ar1a",
              "leftValue": "={{ $json.action }}",
              "rightValue": "a",
              "operator": { "type": "string", "operation": "equals" }
            },
            {
              "id": "ar1t",
              "leftValue": "={{ $json.action }}",
              "rightValue": "t",
              "operator": { "type": "string", "operation": "equals" }
            },
            {
              "id": "ar1r",
              "leftValue": "={{ $json.action }}",
              "rightValue": "r",
              "operator": { "type": "string", "operation": "equals" }
            }
          ],
          "combinator": "or"
        },
        "options": {}
      },
      "id": "w7-action-router-1",
      "name": "w7-action-router-1",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1500, 200]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "ar2",
              "leftValue": "={{ $json.action }}",
              "rightValue": "o",
              "operator": { "type": "string", "operation": "equals" }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "w7-action-router-2",
      "name": "w7-action-router-2",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1500, 400]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "ar3c",
              "leftValue": "={{ $json.action }}",
              "rightValue": "c",
              "operator": { "type": "string", "operation": "equals" }
            },
            {
              "id": "ar3u",
              "leftValue": "={{ $json.action }}",
              "rightValue": "u",
              "operator": { "type": "string", "operation": "equals" }
            },
            {
              "id": "ar3x",
              "leftValue": "={{ $json.action }}",
              "rightValue": "x",
              "operator": { "type": "string", "operation": "equals" }
            },
            {
              "id": "ar3g",
              "leftValue": "={{ $json.action }}",
              "rightValue": "g",
              "operator": { "type": "string", "operation": "equals" }
            },
            {
              "id": "ar3T",
              "leftValue": "={{ $json.action }}",
              "rightValue": "T",
              "operator": { "type": "string", "operation": "equals" }
            }
          ],
          "combinator": "or"
        },
        "options": {}
      },
      "id": "w7-action-router-3",
      "name": "w7-action-router-3",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1500, 600]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "ar4",
              "leftValue": "={{ $json.action }}",
              "rightValue": "b",
              "operator": { "type": "string", "operation": "equals" }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "w7-action-router-4",
      "name": "w7-action-router-4",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1500, 800]
    },
    {
      "parameters": {},
      "id": "w7-noop-unknown-action",
      "name": "w7-noop-unknown-action",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [1700, 1000]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7008-build-mutation\n// Branch on action -> compute next label_ids + Linear GraphQL mutation body.\n// Reached only for original-message actions {a, t, r} via w7-action-router-1.\n// 'o' (Override) is now routed to w7-mint-picker-keyboard instead — its branch\n// here is retained as a defensive fallback only.\nconst data = $input.item.json;\nconst labelsMap = data.labels_map || {};\nconst existingIds = (data.issue_existing_label_ids || []).slice();\nconst chosenWorker = data.chosen_worker;\n\nconst pendingApprovalId = labelsMap['pending-approval'];\nconst triageId = labelsMap['triage'];\nconst manualInterventionId = labelsMap['manual-intervention-required'];\nconst chosenWorkerId = labelsMap[chosenWorker];\n\nconst actionLabelMap = { a: 'Approve', o: 'Override', t: 'Triage', r: 'Request Revision' };\nconst actionLabel = actionLabelMap[data.action] || ('Unknown(' + data.action + ')');\n\nif (!pendingApprovalId) {\n  return { json: { ...data, _build_error: 'pending-approval label id missing from labels_map' } };\n}\n\nlet nextIds = existingIds.filter(id => id !== pendingApprovalId);\n\nif (data.action === 'a') {\n  // Approve: keep worker label, drop pending-approval (already filtered)\n  // No-op beyond the filter.\n} else if (data.action === 'o') {\n  // Defensive fallback: Override should now route to picker-mint.\n  if (chosenWorkerId) nextIds = nextIds.filter(id => id !== chosenWorkerId);\n  if (triageId && !nextIds.includes(triageId)) nextIds.push(triageId);\n} else if (data.action === 't') {\n  // Triage: add triage label\n  if (triageId && !nextIds.includes(triageId)) nextIds.push(triageId);\n} else if (data.action === 'r') {\n  // Request Revision: add manual-intervention-required\n  if (manualInterventionId && !nextIds.includes(manualInterventionId)) nextIds.push(manualInterventionId);\n} else {\n  return { json: { ...data, _build_error: 'unknown action code: ' + data.action } };\n}\n\nconst decided_at = new Date().toISOString();\nconst commentBody = '**W7 callback decided: ' + actionLabel + '**\\n\\n'\n  + 'Tap source: Telegram inline button\\n'\n  + 'Decided at: ' + decided_at + '\\n'\n  + 'Decided by user_id: ' + (data.from_user_id != null ? data.from_user_id : 'unknown') + '\\n'\n  + 'Original proposal: ' + chosenWorker + ' (conf ' + (data.confidence != null ? data.confidence : 'n/a') + ', risk ' + (data.risk || 'n/a') + ')\\n'\n  + 'Callback age at decision: ' + (data.callback_age_seconds != null ? data.callback_age_seconds + 's' : 'unknown') + '\\n\\n'\n  + 'trace_id: ' + (data.trace_id || 'unknown');\n\nconst mutation_body_obj = {\n  query: 'mutation W7Apply($issueId: String!, $labelIds: [String!]!, $commentBody: String!) { issueUpdate(id: $issueId, input: { labelIds: $labelIds }) { success issue { identifier } } commentCreate(input: { issueId: $issueId, body: $commentBody }) { success comment { id } } }',\n  variables: { issueId: data.issue_id, labelIds: nextIds, commentBody }\n};\n\nreturn { json: { ...data, action_label: actionLabel, decided_at, next_label_ids: nextIds, mutation_body_obj } };"
      },
      "id": "w7008-build-mutation",
      "name": "w7008-build-mutation",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1700, 100]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.linear.app/graphql",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "linearApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.mutation_body_obj) }}",
        "options": {}
      },
      "id": "w7009-linear-mutate",
      "name": "w7009-linear-mutate",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1900, 100],
      "credentials": {
        "linearApi": { "id": "{{LINEAR_CRED_ID}}", "name": "linear-n8n" }
      },
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/editMessageText",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ (() => { const d = $('w7008-build-mutation').item.json; const esc = (s) => String(s == null ? '' : s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); return JSON.stringify({ chat_id: d.chat_id, message_id: d.message_id, text: '<b>Decided: ' + esc(d.action_label) + '</b>\\n<i>' + esc(d.issue_identifier || '?') + ' \\u2014 ' + esc(d.decided_at) + '</i>', parse_mode: 'HTML', reply_markup: { inline_keyboard: [] } }); })() }}",
        "options": {}
      },
      "id": "w7010-edit-message",
      "name": "w7010-edit-message",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [2100, 100],
      "continueOnFail": true
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7011-mark-decided\n// Two file appends: pending_callbacks.jsonl 'decided' row + routing_history.jsonl extension row.\nconst fs = require('fs');\nconst PENDING = '/miru-data/pending_callbacks.jsonl';\nconst HISTORY = '/miru-data/routing_history.jsonl';\nconst WORKFLOW_VERSION = 'phase-2-v1';\n\nconst prior = $('w7008-build-mutation').item.json;\nconst editResp = $input.item.json;\n\nconst decided_at = prior.decided_at;\nconst action = prior.action;\nconst action_label = prior.action_label;\nconst token = prior.token;\nconst trace_id = prior.trace_id;\nconst task_id = prior.issue_id;\nconst task_identifier = prior.issue_identifier;\n\n// 1) pending_callbacks.jsonl: 'decided' row\nconst decidedRow = {\n  kind: 'decided',\n  token,\n  action,\n  action_label,\n  decided_at,\n  decided_by_user_id: prior.from_user_id != null ? prior.from_user_id : null,\n  trace_id,\n  task_id,\n  task_identifier,\n  edit_message_ok: !(editResp && editResp.error)\n};\nfs.appendFileSync(PENDING, JSON.stringify(decidedRow) + '\\n');\n\n// 2) routing_history.jsonl: full row matching canon schema + Phase 2 extensions\nconst historyRow = {\n  timestamp: decided_at,\n  trace_id,\n  task_id,\n  task_identifier,\n  extracted_signals: null,\n  ranked_candidates: null,\n  chosen_worker: prior.chosen_worker,\n  confidence: prior.confidence,\n  risk: prior.risk,\n  operator_override_flag: action !== 'a',\n  outcome: 'callback-decided',\n  worker_response_ref: 'callback-' + token,\n  proposed_model_version: null,\n  actual_model_version: null,\n  w2_workflow_version: WORKFLOW_VERSION,\n  callback_source: 'telegram',\n  button_tapped: action_label,\n  button_set: prior.button_set,\n  callback_age_seconds: prior.callback_age_seconds != null ? prior.callback_age_seconds : null,\n  decided_at,\n  decided_by_user_id: prior.from_user_id != null ? prior.from_user_id : null\n};\nfs.appendFileSync(HISTORY, JSON.stringify(historyRow) + '\\n');\n\nreturn { json: { ...prior, _decided_persisted: true, _history_persisted: true } };"
      },
      "id": "w7011-mark-decided",
      "name": "w7011-mark-decided",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2300, 100]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7-mint-picker-keyboard (PRO-80 Phase A)\n// Override tap arrives here. Re-mint a 6-button picker keyboard re-using the\n// existing token (HMAC anchor + pattern shared with w2012a-mint-callback-token).\n// No pending_callbacks row written — the original 'intent' row remains valid;\n// the picker tap eventually writes the 'decided' row.\nconst crypto = require('crypto');\nconst ANCHOR_UNIX = 1767225600;\n\nconst data = $input.item.json;\nconst secret = $env.TELEGRAM_CALLBACK_SECRET;\nif (!secret) throw new Error('TELEGRAM_CALLBACK_SECRET not set');\n\nconst token = data.token;\nconst ts_minutes = Math.floor((Math.floor(Date.now()/1000) - ANCHOR_UNIX) / 60);\nconst ts_hex = ts_minutes.toString(16).padStart(8, '0').slice(-8);\n\nfunction pickerButton(actionCode, label) {\n  const nonce = crypto.randomBytes(4).toString('hex');\n  const payload = token + actionCode + nonce + ts_hex;\n  const hmac = crypto.createHmac('sha256', secret).update(payload).digest('hex').slice(0, 32);\n  const cb = token + actionCode + nonce + ts_hex + hmac;\n  if (cb.length !== 61) throw new Error('picker callback_data length mismatch: ' + cb.length);\n  return { text: label, callback_data: cb };\n}\n\nconst inline_keyboard = [\n  [pickerButton('c', 'Claude Code'), pickerButton('u', 'Cursor'), pickerButton('x', 'Codex')],\n  [pickerButton('g', 'Gemini CLI'), pickerButton('T', 'Triage'), pickerButton('b', '\\u21A9 Back')]\n];\n\nconst telegram_edit_body = {\n  chat_id: data.chat_id,\n  message_id: data.message_id,\n  reply_markup: { inline_keyboard }\n};\n\nreturn { json: { ...data, telegram_edit_body, _picker_minted_at: new Date().toISOString() } };"
      },
      "id": "w7-mint-picker-keyboard",
      "name": "w7-mint-picker-keyboard",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1700, 400]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/editMessageReplyMarkup",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.telegram_edit_body) }}",
        "options": {}
      },
      "id": "w7-edit-to-picker",
      "name": "w7-edit-to-picker",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1900, 400],
      "continueOnFail": true
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7-picker-build-mutation (PRO-80 Phase A)\n// Picker button tap (c/u/x/g/T) — compute next label_ids and Linear mutation\n// for the operator-selected worker (or triage). Maps action codes to canonical\n// Linear label names; gemini is operator-locked decision (PRO-80 prompt).\nconst data = $input.item.json;\n\nconst ACTION_TO_LABEL = {\n  c: 'claude-code',\n  u: 'cursor',\n  x: 'codex',\n  g: 'gemini',\n  T: 'triage'\n};\nconst ACTION_TO_DISPLAY = {\n  c: 'Claude Code',\n  u: 'Cursor',\n  x: 'Codex',\n  g: 'Gemini CLI',\n  T: 'Triage'\n};\n\nconst labelName = ACTION_TO_LABEL[data.action];\nconst display = ACTION_TO_DISPLAY[data.action];\n\nif (!labelName) {\n  return { json: { ...data, _build_error: 'unknown picker action: ' + data.action } };\n}\n\nconst labelsMap = data.labels_map || {};\nconst selectedId = labelsMap[labelName];\nconst pendingApprovalId = labelsMap['pending-approval'];\nconst chosenWorkerId = labelsMap[data.chosen_worker];\nconst KNOWN_WORKER_KEYS = ['claude-code', 'cursor', 'codex', 'gemini', 'gemini'];\n\nif (!selectedId) {\n  return { json: { ...data, _build_error: 'picker label not in labels_map: ' + labelName + ' (ensure label exists in Linear so w2006 picks it up)' } };\n}\nif (!pendingApprovalId) {\n  return { json: { ...data, _build_error: 'pending-approval label id missing from labels_map' } };\n}\n\n// Drop pending-approval, drop any prior worker label, add the selected label.\nlet nextIds = (data.issue_existing_label_ids || []).slice();\nnextIds = nextIds.filter(id => id !== pendingApprovalId);\nfor (const k of KNOWN_WORKER_KEYS) {\n  const wid = labelsMap[k];\n  if (wid) nextIds = nextIds.filter(id => id !== wid);\n}\nif (chosenWorkerId) nextIds = nextIds.filter(id => id !== chosenWorkerId);\nif (!nextIds.includes(selectedId)) nextIds.push(selectedId);\n\nconst overrideFlag = labelName !== data.chosen_worker;\nconst decided_at = new Date().toISOString();\nconst commentBody = '**W7 picker decided: ' + display + '**\\n\\n'\n  + 'Picker selection via Telegram inline button\\n'\n  + 'Decided at: ' + decided_at + '\\n'\n  + 'Decided by user_id: ' + (data.from_user_id != null ? data.from_user_id : 'unknown') + '\\n'\n  + 'W2 proposal: ' + data.chosen_worker + ' (conf ' + (data.confidence != null ? data.confidence : 'n/a') + ', risk ' + (data.risk || 'n/a') + ')\\n'\n  + 'Operator selected: ' + labelName + (overrideFlag ? ' (override)' : ' (matches proposal)') + '\\n'\n  + 'Callback age at decision: ' + (data.callback_age_seconds != null ? data.callback_age_seconds + 's' : 'unknown') + '\\n\\n'\n  + 'trace_id: ' + (data.trace_id || 'unknown');\n\nconst mutation_body_obj = {\n  query: 'mutation W7Picker($issueId: String!, $labelIds: [String!]!, $commentBody: String!) { issueUpdate(id: $issueId, input: { labelIds: $labelIds }) { success issue { identifier } } commentCreate(input: { issueId: $issueId, body: $commentBody }) { success comment { id } } }',\n  variables: { issueId: data.issue_id, labelIds: nextIds, commentBody }\n};\n\nreturn { json: { ...data, picker_label_name: labelName, picker_display: display, override_flag_picker: overrideFlag, decided_at, next_label_ids: nextIds, mutation_body_obj } };"
      },
      "id": "w7-picker-build-mutation",
      "name": "w7-picker-build-mutation",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1700, 600]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.linear.app/graphql",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "linearApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.mutation_body_obj) }}",
        "options": {}
      },
      "id": "w7-picker-linear-mutate",
      "name": "w7-picker-linear-mutate",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1900, 600],
      "credentials": {
        "linearApi": { "id": "{{LINEAR_CRED_ID}}", "name": "linear-n8n" }
      },
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/editMessageText",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ (() => { const d = $('w7-picker-build-mutation').item.json; const esc = (s) => String(s == null ? '' : s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); return JSON.stringify({ chat_id: d.chat_id, message_id: d.message_id, text: '<b>\\u2713 Picker decided: ' + esc(d.picker_display) + '</b>\\n<i>' + esc(d.issue_identifier || '?') + ' \\u2014 proposed ' + esc(d.chosen_worker) + ' \\u2014 ' + esc(d.decided_at) + '</i>', parse_mode: 'HTML', reply_markup: { inline_keyboard: [] } }); })() }}",
        "options": {}
      },
      "id": "w7-picker-edit-message",
      "name": "w7-picker-edit-message",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [2100, 600],
      "continueOnFail": true
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7-picker-mark-decided (PRO-80 Phase A)\n// Final-stage picker decision: append pending_callbacks 'decided' row (so any\n// further taps on the same token are rejected as already_decided) AND append a\n// routing_history row with the new picker fields (proposed_worker, selected_worker, override).\nconst fs = require('fs');\nconst PENDING = '/miru-data/pending_callbacks.jsonl';\nconst HISTORY = '/miru-data/routing_history.jsonl';\nconst WORKFLOW_VERSION = 'phase-2-v1';\n\nconst prior = $('w7-picker-build-mutation').item.json;\nconst editResp = $input.item.json;\n\nconst decided_at = prior.decided_at;\nconst action = prior.action;\nconst action_label = prior.picker_display;\nconst token = prior.token;\nconst trace_id = prior.trace_id;\nconst task_id = prior.issue_id;\nconst task_identifier = prior.issue_identifier;\nconst proposed_worker = prior.chosen_worker;\nconst selected_worker = prior.picker_label_name;\nconst override = !!prior.override_flag_picker;\n\n// 1) pending_callbacks.jsonl: 'decided' row, source='picker'\nconst decidedRow = {\n  kind: 'decided',\n  token,\n  action,\n  action_label,\n  decided_at,\n  decided_by_user_id: prior.from_user_id != null ? prior.from_user_id : null,\n  trace_id,\n  task_id,\n  task_identifier,\n  edit_message_ok: !(editResp && editResp.error),\n  source: 'picker'\n};\nfs.appendFileSync(PENDING, JSON.stringify(decidedRow) + '\\n');\n\n// 2) routing_history.jsonl: outcome='picker-decided' with PRO-80 fields\nconst historyRow = {\n  timestamp: decided_at,\n  trace_id,\n  task_id,\n  task_identifier,\n  extracted_signals: null,\n  ranked_candidates: null,\n  chosen_worker: proposed_worker,\n  confidence: prior.confidence,\n  risk: prior.risk,\n  operator_override_flag: override,\n  outcome: 'picker-decided',\n  worker_response_ref: 'callback-picker-' + token,\n  proposed_model_version: null,\n  actual_model_version: null,\n  w2_workflow_version: WORKFLOW_VERSION,\n  callback_source: 'telegram',\n  button_tapped: action_label,\n  button_set: 'picker',\n  callback_age_seconds: prior.callback_age_seconds != null ? prior.callback_age_seconds : null,\n  decided_at,\n  decided_by_user_id: prior.from_user_id != null ? prior.from_user_id : null,\n  proposed_worker,\n  selected_worker,\n  override\n};\nfs.appendFileSync(HISTORY, JSON.stringify(historyRow) + '\\n');\n\nreturn { json: { ...prior, _decided_persisted: true, _history_persisted: true } };"
      },
      "id": "w7-picker-mark-decided",
      "name": "w7-picker-mark-decided",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2300, 600]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// w7-mint-original-keyboard (PRO-80 Phase A)\n// Back tap: re-mint the original 4-button keyboard (a/o/t/r) with fresh HMACs.\n// Re-uses the same token; new nonces and ts_hex.\nconst crypto = require('crypto');\nconst ANCHOR_UNIX = 1767225600;\n\nconst data = $input.item.json;\nconst secret = $env.TELEGRAM_CALLBACK_SECRET;\nif (!secret) throw new Error('TELEGRAM_CALLBACK_SECRET not set');\n\nconst token = data.token;\nconst ts_minutes = Math.floor((Math.floor(Date.now()/1000) - ANCHOR_UNIX) / 60);\nconst ts_hex = ts_minutes.toString(16).padStart(8, '0').slice(-8);\n\nfunction origButton(actionCode, label) {\n  const nonce = crypto.randomBytes(4).toString('hex');\n  const payload = token + actionCode + nonce + ts_hex;\n  const hmac = crypto.createHmac('sha256', secret).update(payload).digest('hex').slice(0, 32);\n  const cb = token + actionCode + nonce + ts_hex + hmac;\n  if (cb.length !== 61) throw new Error('original-keyboard callback_data length mismatch: ' + cb.length);\n  return { text: label, callback_data: cb };\n}\n\nconst inline_keyboard = [\n  [origButton('a', 'Approve'), origButton('o', 'Override')],\n  [origButton('t', 'Triage'), origButton('r', 'Request Revision')]\n];\n\nconst telegram_edit_body = {\n  chat_id: data.chat_id,\n  message_id: data.message_id,\n  reply_markup: { inline_keyboard }\n};\n\nreturn { json: { ...data, telegram_edit_body, _back_minted_at: new Date().toISOString() } };"
      },
      "id": "w7-mint-original-keyboard",
      "name": "w7-mint-original-keyboard",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1700, 800]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/editMessageReplyMarkup",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.telegram_edit_body) }}",
        "options": {}
      },
      "id": "w7-edit-to-original",
      "name": "w7-edit-to-original",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1900, 800],
      "continueOnFail": true
    }
  ],
  "connections": {
    "w7001-telegram-trigger": {
      "main": [[{"node": "w7002-ack-callback", "type": "main", "index": 0}]]
    },
    "w7002-ack-callback": {
      "main": [[{"node": "w7003-parse-payload", "type": "main", "index": 0}]]
    },
    "w7003-parse-payload": {
      "main": [[{"node": "w7004-hmac-validate", "type": "main", "index": 0}]]
    },
    "w7004-hmac-validate": {
      "main": [[{"node": "w7005-validate-branch", "type": "main", "index": 0}]]
    },
    "w7005-validate-branch": {
      "main": [
        [{"node": "w7006-lookup-pending", "type": "main", "index": 0}],
        [{"node": "w7-noop-rejected", "type": "main", "index": 0}]
      ]
    },
    "w7006-lookup-pending": {
      "main": [[{"node": "w7007-found-branch", "type": "main", "index": 0}]]
    },
    "w7007-found-branch": {
      "main": [
        [{"node": "w7-action-router-1", "type": "main", "index": 0}],
        [{"node": "w7-noop-duplicate", "type": "main", "index": 0}]
      ]
    },
    "w7-action-router-1": {
      "main": [
        [{"node": "w7008-build-mutation", "type": "main", "index": 0}],
        [{"node": "w7-action-router-2", "type": "main", "index": 0}]
      ]
    },
    "w7-action-router-2": {
      "main": [
        [{"node": "w7-mint-picker-keyboard", "type": "main", "index": 0}],
        [{"node": "w7-action-router-3", "type": "main", "index": 0}]
      ]
    },
    "w7-action-router-3": {
      "main": [
        [{"node": "w7-picker-build-mutation", "type": "main", "index": 0}],
        [{"node": "w7-action-router-4", "type": "main", "index": 0}]
      ]
    },
    "w7-action-router-4": {
      "main": [
        [{"node": "w7-mint-original-keyboard", "type": "main", "index": 0}],
        [{"node": "w7-noop-unknown-action", "type": "main", "index": 0}]
      ]
    },
    "w7008-build-mutation": {
      "main": [[{"node": "w7009-linear-mutate", "type": "main", "index": 0}]]
    },
    "w7009-linear-mutate": {
      "main": [[{"node": "w7010-edit-message", "type": "main", "index": 0}]]
    },
    "w7010-edit-message": {
      "main": [[{"node": "w7011-mark-decided", "type": "main", "index": 0}]]
    },
    "w7-mint-picker-keyboard": {
      "main": [[{"node": "w7-edit-to-picker", "type": "main", "index": 0}]]
    },
    "w7-picker-build-mutation": {
      "main": [[{"node": "w7-picker-linear-mutate", "type": "main", "index": 0}]]
    },
    "w7-picker-linear-mutate": {
      "main": [[{"node": "w7-picker-edit-message", "type": "main", "index": 0}]]
    },
    "w7-picker-edit-message": {
      "main": [[{"node": "w7-picker-mark-decided", "type": "main", "index": 0}]]
    },
    "w7-mint-original-keyboard": {
      "main": [[{"node": "w7-edit-to-original", "type": "main", "index": 0}]]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
