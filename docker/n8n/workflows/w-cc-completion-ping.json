{
  "name": "CC Completion Ping",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            { "field": "seconds", "secondsInterval": 60 }
          ]
        }
      },
      "id": "ccp001-schedule-trigger",
      "name": "ccp001-schedule-trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [200, 300]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForAllItems",
        "jsCode": "// ccp002-read-and-diff\n// Tail-read /miru-data/cc_completion_log.jsonl and compare row count to last\n// seen count stored in $getWorkflowStaticData('global').cc_completion_last_count.\n// Emits one item with {new_rows, current_count, regressed, last_count}.\n//\n// Restart-safe: n8n persists 'global' static data across container restarts when\n// the execution finishes successfully (no exceptions downstream).\n//\n// First-run init: when last_count is undefined (workflow never ran before), seed\n// it to current row count and emit empty new_rows. Avoids backfiring on every\n// historical row at first activation (covers BOOTSTRAP + any prior real markers).\n//\n// Regression handling: if current_count < last_count, the file appears truncated\n// or replaced. This violates the append-only convention. Emit regressed=true\n// so the downstream alert fires; do NOT update last_count (operator decides).\nconst fs = require('fs');\nconst PATH = '/miru-data/cc_completion_log.jsonl';\n\nconst staticData = $getWorkflowStaticData('global');\nconst lastCount = staticData.cc_completion_last_count;\n\nlet content = '';\ntry {\n  content = fs.readFileSync(PATH, 'utf8');\n} catch (e) {\n  if (e && e.code === 'ENOENT') {\n    return [{ json: { new_rows: [], current_count: 0, regressed: false, last_count: lastCount == null ? 0 : lastCount, _file_missing: true } }];\n  }\n  throw e;\n}\n\nconst rows = content.split('\\n').filter(line => line.trim().length > 0);\nconst currentCount = rows.length;\n\nif (lastCount == null) {\n  staticData.cc_completion_last_count = currentCount;\n  return [{ json: { new_rows: [], current_count: currentCount, regressed: false, last_count: currentCount, _init: true } }];\n}\n\nif (currentCount < lastCount) {\n  return [{ json: { new_rows: [], current_count: currentCount, regressed: true, last_count: lastCount } }];\n}\n\nconst newRows = rows.slice(lastCount);\nstaticData.cc_completion_last_count = currentCount;\nreturn [{ json: { new_rows: newRows, current_count: currentCount, regressed: false, last_count: currentCount } }];"
      },
      "id": "ccp002-read-and-diff",
      "name": "ccp002-read-and-diff",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [400, 300]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "rb1",
              "leftValue": "={{ $json.regressed }}",
              "rightValue": true,
              "operator": { "type": "boolean", "operation": "true", "singleValue": true }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "ccp003-regressed-branch",
      "name": "ccp003-regressed-branch",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [600, 300]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// ccp004a-build-regression-msg\n// File regressed below last-seen count. Build a single Telegram alert body.\nconst data = $input.item.json;\nconst text = '\\u26A0 <b>cc_completion_log.jsonl regressed</b>\\nrows now=' + (data.current_count != null ? data.current_count : '?') + ', last_seen=' + (data.last_count != null ? data.last_count : '?') + '\\n<i>append-only convention violated; manual investigation required</i>';\nconst telegram_body = {\n  chat_id: $env.TELEGRAM_CHAT_ID,\n  text,\n  parse_mode: 'HTML',\n  disable_notification: false\n};\nreturn { json: { ...data, telegram_body } };"
      },
      "id": "ccp004a-build-regression-msg",
      "name": "ccp004a-build-regression-msg",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [800, 200]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/sendMessage",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.telegram_body) }}",
        "options": {}
      },
      "id": "ccp004b-send-regression",
      "name": "ccp004b-send-regression",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1000, 200],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "fieldToSplitOut": "new_rows",
        "options": {}
      },
      "id": "ccp005-split-rows",
      "name": "ccp005-split-rows",
      "type": "n8n-nodes-base.splitOut",
      "typeVersion": 1,
      "position": [800, 400]
    },
    {
      "parameters": {
        "language": "javaScript",
        "mode": "runOnceForEachItem",
        "jsCode": "// ccp006-parse-and-build\n// Parse one marker row, filter out non-PRO ticket_ids (BOOTSTRAP, etc.), build\n// a Telegram message body shaped per PRO-99 spec. Sets _skip=true for rows that\n// shouldn't ping (BOOTSTRAP, malformed JSON, unknown status).\n//\n// Filter: ticket_id must start with 'PRO-' (matches both real PRO-99 and synthetic\n// PRO-TEST-99-1 test rows; excludes BOOTSTRAP and any future non-ticket markers).\nfunction esc(s) { return String(s == null ? '' : s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }\n\nconst data = $input.item.json;\nconst rowStr = data.new_rows;\n\nlet row;\ntry { row = JSON.parse(rowStr); }\ncatch (e) {\n  return { json: { _skip: true, _skip_reason: 'invalid JSON: ' + (e && e.message ? e.message : 'unknown'), raw: rowStr } };\n}\n\nconst ticketId = row.ticket_id || '';\nif (!/^PRO-/.test(ticketId)) {\n  return { json: { _skip: true, _skip_reason: 'non-PRO ticket_id: ' + ticketId } };\n}\n\nconst status = row.status || '';\nlet icon;\nif (status === 'CONFIRMED_WORKING') { icon = '\\u2705'; }\nelse if (status === 'INCONCLUSIVE') { icon = '\\u26A0'; }\nelse if (status === 'FAILED') { icon = '\\u274C \\uD83D\\uDEA8'; }\nelse { return { json: { _skip: true, _skip_reason: 'unknown status: ' + status } }; }\n\nconst summary = esc(row.summary || '');\nconst linearState = row.linear_state_after;\nconst prNumber = row.pr_number;\nconst mergeSha = row.merge_commit_sha;\n\nlet thirdLine = '';\nif (linearState) {\n  const parts = ['<i>' + esc(linearState) + '</i>'];\n  if (prNumber) parts.push('<a href=\"https://github.com/Dreighto/project-miru/pull/' + Number(prNumber) + '\">PR #' + Number(prNumber) + '</a>');\n  if (mergeSha && typeof mergeSha === 'string') parts.push(esc(mergeSha.slice(0, 7)));\n  thirdLine = parts.join(' \\u00B7 ');\n}\n\nconst text = icon + ' <b>' + esc(ticketId) + ' done</b>\\n' + summary + (thirdLine ? '\\n' + thirdLine : '');\n\nconst telegram_body = {\n  chat_id: $env.TELEGRAM_CHAT_ID,\n  text,\n  parse_mode: 'HTML',\n  disable_notification: false\n};\n\nreturn { json: { _skip: false, ticket_id: ticketId, status, telegram_body } };"
      },
      "id": "ccp006-parse-and-build",
      "name": "ccp006-parse-and-build",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1000, 400]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "id": "sb1",
              "leftValue": "={{ $json._skip }}",
              "rightValue": false,
              "operator": { "type": "boolean", "operation": "false", "singleValue": true }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "ccp007-skip-branch",
      "name": "ccp007-skip-branch",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1200, 400]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/sendMessage",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.telegram_body) }}",
        "options": {}
      },
      "id": "ccp008-send-telegram",
      "name": "ccp008-send-telegram",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1400, 400],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {},
      "id": "ccp-noop-skipped",
      "name": "ccp-noop-skipped",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [1400, 560]
    }
  ],
  "connections": {
    "ccp001-schedule-trigger": {
      "main": [[{"node": "ccp002-read-and-diff", "type": "main", "index": 0}]]
    },
    "ccp002-read-and-diff": {
      "main": [[{"node": "ccp003-regressed-branch", "type": "main", "index": 0}]]
    },
    "ccp003-regressed-branch": {
      "main": [
        [{"node": "ccp004a-build-regression-msg", "type": "main", "index": 0}],
        [{"node": "ccp005-split-rows", "type": "main", "index": 0}]
      ]
    },
    "ccp004a-build-regression-msg": {
      "main": [[{"node": "ccp004b-send-regression", "type": "main", "index": 0}]]
    },
    "ccp005-split-rows": {
      "main": [[{"node": "ccp006-parse-and-build", "type": "main", "index": 0}]]
    },
    "ccp006-parse-and-build": {
      "main": [[{"node": "ccp007-skip-branch", "type": "main", "index": 0}]]
    },
    "ccp007-skip-branch": {
      "main": [
        [{"node": "ccp008-send-telegram", "type": "main", "index": 0}],
        [{"node": "ccp-noop-skipped", "type": "main", "index": 0}]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "errorWorkflow": "l5wzFuWnJ2zSoMM2"
  }
}
