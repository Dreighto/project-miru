# Drop-zone for n8n W4 prompt artifacts and `<trace_id>.result.json` receipts.
# Created in PRO-83. Runtime artifacts inside this directory are gitignored;
# this .gitkeep keeps the directory tracked.
