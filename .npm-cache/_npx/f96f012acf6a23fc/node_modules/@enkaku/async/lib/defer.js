function noop() {}
/**
 * Create a Deferred object.
 */ export function defer() {
    let resolve = noop;
    let reject = noop;
    const promise = new Promise((res, rej)=>{
        resolve = res;
        reject = rej;
    });
    return {
        promise,
        resolve,
        reject
    };
}
