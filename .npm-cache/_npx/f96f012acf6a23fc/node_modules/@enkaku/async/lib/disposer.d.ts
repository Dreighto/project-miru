export type DisposerParams = {
    dispose?: (reason?: unknown) => Promise<void>;
    signal?: AbortSignal;
};
/**
 * Disposer class, providing a dispose function and a disposed Promise.
 */
export declare class Disposer extends AbortController implements AsyncDisposable {
    #private;
    constructor(params?: DisposerParams);
    get disposed(): Promise<void>;
    dispose(reason?: unknown): Promise<void>;
    [Symbol.asyncDispose](): Promise<void>;
}
//# sourceMappingURL=disposer.d.ts.map