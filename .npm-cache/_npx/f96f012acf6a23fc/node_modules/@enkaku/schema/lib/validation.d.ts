import type { StandardSchemaV1 } from '@standard-schema/spec';
import type { FromSchema } from 'json-schema-to-ts';
import type { Schema } from './types.js';
/**
 * Validator function, returning a Result of the validation.
 */
export type Validator<T> = (value: unknown) => StandardSchemaV1.Result<T>;
/**
 * Validator function factory using a JSON schema.
 */
export declare function createValidator<S extends Schema, T = FromSchema<S>>(schema: S): Validator<T>;
/**
 * Asserts the type of the given `value` using the `validator`.
 */
export declare function assertType<T>(validator: Validator<T>, value: unknown): asserts value is T;
/**
 * Asserts the type of the given `value` using the `validator` and returns it.
 */
export declare function asType<T>(validator: Validator<T>, value: unknown): T;
/**
 * Checks the type of the given `value` using the `validator`.
 */
export declare function isType<T>(validator: Validator<T>, value: unknown): value is T;
/**
 * Turn a `Validator` function into a standard schema validator.
 */
export declare function toStandardValidator<T>(validator: Validator<T>): StandardSchemaV1<T>;
/**
 * Create a standard schema validator.
 */
export declare function createStandardValidator<S extends Schema, T = FromSchema<S>>(schema: S): StandardSchemaV1<T>;
//# sourceMappingURL=validation.d.ts.map