# Miru skills (parked project)

Miru is not in the active work cycle. Kernel skills that used to live here
were removed on 2026-07-31 because they duplicated (or diverged from) the
canonical copies in other repos.

## Canonical homes

| Concern                                            | Where                                                               |
| -------------------------------------------------- | ------------------------------------------------------------------- |
| Kernel / dispatch / n8n / handoff / DGAS / tickets | `~/dev/LogueOS-Orchestrator/.claude/skills/`                        |
| Console visual language / frontend systems         | `~/dev/LogueOS-Console/.claude/skills/` and `~/dev/knowledge-base/` |
| Cross-project operator skills                      | `~/.claude/skills/`                                                 |

## Skills kept here (Miru domain only)

- `card-corpus-training-prep`
- `db-schema-discipline`
- `op-tcg-domain-glossary`
- `hub-ui-deploy`
- `immersive-ui-craft`
- `ui-visual-verify`
- `research-digest`

When Miru work resumes, load these plus the Orchestrator kernel skills.
Do not re-copy kernel skills into this tree.
